{
  "name": "testing",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "prepare": "pnpm build",
    "build": "../gradlew compileJava && tsc -p tsconfig.json",
    "lint": "../gradlew ktlintCheck && uv run pylint **/*.py --rcfile=../.pylintrc && eslint src/"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
