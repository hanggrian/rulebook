{
  "name": "testing",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "pnpm build",
    "build": "tsc -p tsconfig.json",
    "lint": "uv run pylint **/*.py --rcfile=../.pylintrc && eslint src/"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "rulebook-eslint": "workspace:*",
    "rulebook-typescript-eslint": "workspace:*"
  }
}
