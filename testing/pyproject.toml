[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "testing"
version = "1"
dependencies = [
  "pylint==4.0.5",
  "astroid==4.0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["testing"]
