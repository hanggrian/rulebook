{
  "private": true,
  "scripts": {
    "lint": "uv run cppcheck --enable=style,performance,portability --addon=custom_addon.json c/*.c cpp/*.cpp && uv run pylint **/*.py --rcfile=custom_pylintrc && eslint javascript/ && pnpm exec stylebook *.json"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
