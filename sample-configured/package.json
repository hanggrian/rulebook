{
  "private": true,
  "scripts": {
    "lint": "../gradlew checkstyleMain codenarcMain ktlintCheck && uv run pylint **/*.py --rcfile=custom_pylintrc && eslint javascript/&& uv run cppcheck --enable=style,performance,portability --addon=custom_addon.json c/*.c cpp/*.cpp && pnpm exec stylebook *.json"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
