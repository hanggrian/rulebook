{
  "private": true,
  "scripts": {
    "build": "exit 0",
    "lint": "uv run pylint rulebook_pylint/ --rcfile=../.pylintrc",
    "test": "uv run pytest",
    "coverage": "uv run pytest --cov=."
  }
}
