{
  "private": true,
  "scripts": {
    "lint": "uv run pylint **/*.py --rcfile=../.pylintrc && uv run stylebook *.toml && pnpm exec stylebook *.json",
    "test": "uv run pytest",
    "coverage": "uv run pytest --cov=."
  }
}
