{
  "private": true,
  "scripts": {
    "prepare": "pnpm build",
    "build": "../gradlew compileKotlin",
    "lint": "../gradlew ktlintCheck",
    "test": "../gradlew test",
    "coverage": "../gradlew koverXmlReport"
  }
}
