{
  "name": "stress-test",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "uv run pylint **/*.py --rcfile=../.pylintrc && eslint src/",
    "test": "uv run pytest && vitest"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "rulebook-eslint": "workspace:*",
    "rulebook-typescript-eslint": "workspace:*",
    "testing": "workspace:*"
  }
}
