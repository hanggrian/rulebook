[tool.uv.workspace]
members = [
  "rulebook-cppcheck",
  "rulebook-pylint",
  "codecheck",
  "testing",
  "stress-test",
]

[tool.uv.sources]
rulebook-cppcheck = { workspace = true }
rulebook-pylint = { workspace = true }
testing = { workspace = true }

[dependency-groups]
dev = [
  "pdoc",
  # test
  "pytest",
  "pytest-cov",
]
