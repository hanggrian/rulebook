package com.example.kotlin

class ComplicatedSizeComparison {
    fun foo(foo: List<Int>) {
        if (foo.isEmpty()) {
            println()
        }
        if (foo.isNotEmpty()) {
            println()
        }
    }
}
