package com.example.kotlin

class ComplicatedSizeEquality {
    fun foo(foo: List<Int>) {
        if (foo.isEmpty()) {
            println()
        }
        if (foo.isNotEmpty()) {
            println()
        }
    }
}
