package com.example.kotlin

import kotlin.math.abs

class ComplicatedBooleanComparison {
    fun foo(foo: Boolean) {
        if (foo) {
            println()
        }
    }

    fun bar(bar: Boolean) {
        if (!(bar)) {
            println()
        }
    }
}
