{
  "private": true,
  "scripts": {
    "lint": "uv run cppcheck --enable=style,performance,portability --addon=../addon.json c/*.c cpp/*.cpp && uv run pylint **/*.py --rcfile=../.pylintrc && eslint javascript/ typescript/ && pnpm exec stylebook *.json"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
