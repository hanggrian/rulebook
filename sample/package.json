{
  "private": true,
  "scripts": {
    "lint": "../gradlew checkstyleMain codenarcMain codenarcScript ktlintCheck && uv run pylint **/*.py --rcfile=../.pylintrc && eslint javascript/ typescript/ && uv run cppcheck --enable=performance,portability,style,warning --addon=../addon.json c/*.c cpp/*.cpp"
  }
}
