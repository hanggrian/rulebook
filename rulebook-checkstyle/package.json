{
  "private": true,
  "scripts": {
    "prepare": "pnpm build",
    "build": "../gradlew compileKotlin",
    "lint": "../gradlew ktlintCheck && pnpm exec stylebook *.json",
    "test": "../gradlew test",
    "coverage": "../gradlew koverXmlReport"
  }
}
