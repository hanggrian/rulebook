[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "codecheck"
version = "1"
dependencies = [
  # main
  "pylint==4.0.5",
  "astroid==4.0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["codecheck"]
