[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "codecheck"
version = "1"
dependencies = [
  "pylint==4.0.4",
  "astroid==4.0.2",
]

[tool.hatch.build.targets.wheel]
packages = ["codecheck"]
