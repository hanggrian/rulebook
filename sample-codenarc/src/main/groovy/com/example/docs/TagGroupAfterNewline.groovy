package com.example.docs

/**
 * Just a box.
 *
 * @param width horizontal.
 * @param height vertical.
 */
int summary(int width, int height) {
    return width + height
}

/**
 * [Box].
 *
 * @param width horizontal.
 * @param height vertical.
 */
int justLink(int width, int height) {
    return width + height
}

/**
 * `Box`.
 *
 * @param width horizontal.
 * @param height vertical.
 */
int justCode(int width, int height) {
    return width + height
}
